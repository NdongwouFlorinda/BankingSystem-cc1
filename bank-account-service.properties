# -------------------------------------------------------------
# 🗄️ Configuration de la base de données MySQL
# -------------------------------------------------------------
# L’URL de connexion à la base MySQL
# ⚠️ Remplace localhost par le nom du conteneur Docker si les deux tournent dans Docker Compose.
spring.datasource.url=jdbc:mysql://localhost:3306/account_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true

# Nom d’utilisateur et mot de passe pour la base
spring.datasource.username=root
spring.datasource.password=rootpass

# Driver JDBC de MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# -------------------------------------------------------------
# 🧩 Configuration JPA / Hibernate
# -------------------------------------------------------------
# Hibernate crée ou met à jour automatiquement les tables
spring.jpa.hibernate.ddl-auto=update

# Affiche les requêtes SQL dans la console
spring.jpa.show-sql=true

# Spécifie le dialecte MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# -------------------------------------------------------------
# 🌍 Configuration du serveur web intégré (Tomcat)
# -------------------------------------------------------------
server.port=8092

# -------------------------------------------------------------
# 🪵 Niveau de logs pour Hibernate et SQL
# -------------------------------------------------------------
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
